import React from 'react'

const Categories = ({items}) => {
    return (
        <div class='categories'>
        <ul>
            {items.map((item)=>{
                    {item}
             <li>
                
             </li>
            })}
        </ul>
    </div>
    )
}

export default Categories;
